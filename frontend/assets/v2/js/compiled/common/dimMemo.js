// Generated by CoffeeScript 1.4.0
var DimMemoizer, dimMemo,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

DimMemoizer = (function() {

  function DimMemoizer() {
    this.onResize = __bind(this.onResize, this);

    this.getHeight = __bind(this.getHeight, this);
    this.storage = {};
  }

  DimMemoizer.prototype.getHeight = function(selector) {
    if (this.storage[selector] != null) {
      return this.storage[selector];
    }
    this.storage[selector] = $(selector).height();
    return this.storage[selector];
  };

  DimMemoizer.prototype.onResize = function() {
    return this.storage = {};
  };

  return DimMemoizer;

})();

dimMemo = new DimMemoizer;

$(window).resize(dimMemo.onResize);
