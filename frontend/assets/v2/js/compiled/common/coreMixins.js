// Generated by CoffeeScript 1.4.0
var RoomsContainerMixin,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

RoomsContainerMixin = (function() {

  function RoomsContainerMixin() {
    this.addRoom = __bind(this.addRoom, this);

  }

  RoomsContainerMixin.prototype.getRooms = function() {
    return this.rooms();
  };

  RoomsContainerMixin.prototype.addRoom = function() {
    if (this.rooms.length === 4) {
      return;
    }
    if (this.overall() > 8) {
      return;
    }
    return this.rooms.push(new SpRoom(this));
  };

  RoomsContainerMixin.prototype.removeRoom = function(room) {
    if (this.rooms().length === 1) {
      return;
    }
    return this.rooms.splice(this.rooms.indexOf(room), 1);
  };

  RoomsContainerMixin.prototype.onOverallChanged = function(clb) {
    return this.overall.subscribe(clb);
  };

  return RoomsContainerMixin;

})();

implement(RoomsContainerMixin, IRoomsContainer);
